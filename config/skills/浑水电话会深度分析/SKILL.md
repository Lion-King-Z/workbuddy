---
name: 浑水电话会深度分析
description: |
  每日接收浑水调研电话会手动总结，自动完成产业瓶颈分析、景气度扫描、信源交叉验证，沉淀到IMA「聚宝盆」知识库。
  所有产业链深度研究报告、金融投资分析，研究完成后也同步到聚宝盆。
  触发条件：用户发送电话会总结文本时激活。用户说「整理电话会」「浑水调研」「机构电话会」「今天会议」等。产业链研究完成后自动同步。
agent_created: true
---

# 浑水电话会深度分析 Skill

将浑水调研每日电话会总结，固化为标准化的四步投研管线——记录→瓶颈→景气度→信源验证→**IMA「聚宝盆」知识库（主输出）**。

## 核心理念

机构电话会是最接近产业真相的一手信息源。卖方研报有时滞、社群观点有噪音，但电话会里的买方提问和卖方回答，能直接反映当前市场最关心的供需矛盾与定价权变化。本skill确保每一份电话会都不被浪费。

## 触发条件

当用户发送浑水调研电话会总结文本时激活，典型格式：
- 「今天浑水电话会讲了XXX」
- 「昨天电话会总结：XXX」
- 用户直接粘贴电话会文字稿/要点

## 存储策略

### 本地文件夹结构

```
G:\锅师\
├── 深度研究/                      ← 产业链研报 + 周期产出
│   └── MMDD-MMDD/                ← 周文件夹
├── 浑水调研/                      ← 浑水生态（电话会/景气度/紧缺度/跟踪）
│   ├── MMDD-MMDD/                ← 周文件夹
│   └── 浑水跟踪_YYYY-MM.md       ← 月度跟踪
├── 聚宝盆索引.md                  ← 本地索引
├── 信息源清单.md
└── 年报_YYYY.md
```

### 电话会存储策略

| 文件 | 用途 | 格式 |
|------|------|------|
| `YYYY-MM-DD_电话会_关键词.md` | **每日独立完整分析文件** | 瓶颈+景气度+信源+操作建议 |
| `浑水跟踪_YYYY-MM.md` | 月度总账（只保留两张表） | 累计信号强度 + S级信号汇总 |

**原则**：每日分析独立成文件，月度总账只做汇总索引。打开文件夹按日期排列，最新在最上面。

### 输出层级

| 输出 | 用途 | 时机 |
|------|------|------|
| **IMA「聚宝盆」** | 主输出，完整研究结果（ID: 9P9LfmFJRFwLidUrzKQu0cZZ5QMVxtv61QA9iFoV90g=） | 每次研究必写 |
| **聚宝盆索引.md** | 本地检索索引，追加一行 | 每次同步后自动更新 |
| **本地分类MD** | 本地备份，存入对应文件夹 | 每次产出必存 |

## 工作流程

### 阶段一：接收与解析

从用户发送的文本中提取结构化信息：

```
## 会议元数据
- 日期：
- 涉及行业/公司：
- 核心议题：

## 关键信息点
1. [供需变化]
2. [价格信号]
3. [产能扩张/收缩]
4. [买方关注焦点]
```

### 阶段二：产业瓶颈分析

对每个涉及的行业/环节，调用「产业链瓶颈投研skill」的方法论：

1. **卡点定位**：该环节在整个产业链中的不可替代性
2. **供给刚性判断**：产能扩张周期（6个月/12个月/24个月以上？）
3. **需求增速验证**：下游需求是否在加速/放缓
4. **价格传导**：上游涨价是否能向下游传导

🔴 **CHECKPOINT**：每个行业/环节四项分析全部覆盖后再进入阶段三。

### 阶段三：景气度扫描

调用「产业链瓶颈投研skill」步骤4b的方法论（已融合景气度扫描框架）：

1. **景气度方向**：上行/持平/下行
2. **供给端信号**：产能利用率、库存水平、扩产计划
3. **需求端信号**：订单可见度、下游扩产、客户集中度变化
4. **紧缺度评分**（1-5分）

### 阶段四：信源交叉验证

调用「信源检查skill」的分级标准：

**信源权重优先级**：浑水调研电话会（主力） > WebSearch公开数据 > IMA锅师信息沉淀/其他社群（仅辅助对照）

| 等级 | 定义 | 操作 |
|------|------|------|
| **A** | 电话会直接数据 + 公开数据一致 | 直接采信 |
| **B** | 电话会主观判断 + 其他信源佐证 | 标注"参考" |
| **C** | 电话会单一判断 + 无其他信源 | 标注"待验证" |

对每个关键结论标注信源等级。当出现A级矛盾（两个A级信源互相矛盾）→ 暂停输出，请求用户确认。

🛑 **STOP**：所有结论必须≥C级才能进入阶段五。D/E级存在→暂停，向用户报告。

### 阶段五：沉淀（IMA主输出 + 本地备份）

执行顺序：**先确认本地文件名和路径正确 → 再同步IMA知识库**。

#### 5A. 写入IMA「聚宝盆」知识库（主输出）

**必做**，每次电话会分析完成立即执行。

- 知识库ID：`9P9LfmFJRFwLidUrzKQu0cZZ5QMVxtv61QA9iFoV90g=`
- 名称：聚宝盆

使用 IMA MCP/imap API 写入：先通过 `import_doc` 创建笔记，再通过 `add_knowledge`（`media_type=11`）关联到聚宝盆知识库。

**IMA笔记内容格式**：

```
标题：浑水电话会 [日期] — [主线关键词]

## 关键信息
- [3-5条核心信号]

## 产业瓶颈
| 环节 | 卡点 | 供给刚性 | 不可替代性 |
|------|------|----------|-----------|
| ... | ... | ... | ... |

## 景气度
| 环节 | 方向 | 紧缺度 | 核心信号 |
|------|------|--------|----------|
| ... | ... | X/5 | ... |

## 信源验证
| 结论 | 等级 | 验证结果 |
|------|------|----------|
| ... | A/B/C | ✅/⚠ |

## 操作优先级
| 优先级 | 标的 | 逻辑 | 风险 |
|--------|------|------|------|
| ... | ... | ... | ... |

## 下一条跟踪要点
1. ...
```

#### 5B. 追加本地月度总账（备份）

将相同内容追加到 `浑水跟踪_YYYY-MM.md`。

## 月度跟踪文件格式

````markdown
# 浑水电话会跟踪 — 2026年06月

## 累计信号强度

| 行业/环节 | 提及频次 | 景气度方向 | 紧缺度 | 最新信号日 |
|-----------|---------|-----------|--------|-----------|
| 示例: MLCC | 5次 | ↑ 上行 | 4/5 | 06-11 |

---

### 06-11（周三）

**涉及**：半导体、MLCC、...

**关键信息**：
- ...

**瓶颈分析**：
- 卡点：[环节]，不可替代性：[判断]，供给刚性：[是/否，周期]

**景气度**：
- 方向：↑/→/↓
- 紧缺度：X/5
- 核心信号：[一句话]

**信源验证**：
- [结论] — 等级 A/B/C — [验证渠道]

**操作建议**：
- [关注/忽略/深度研究/直接买入]
````

## S级信号判定标准

满足以下任一条件即为S级：

| 信号 | 判断标准 |
|------|----------|
| **供给断崖** | 某环节产能收缩30%+ 且扩产周期>18个月 |
| **定价权突变** | 某环节成功提价20%+ 且下游接受 |
| **需求跳升** | 下游需求增速环比翻倍（电话会多方交叉确认） |
| **产业拐点** | 连续3次电话会景气度方向一致反转 |

S级信号产出：已进入IMA笔记（自动） + 独立深度报告（额外生成）

## IMA同步技术规范（2026-06-14 沉淀）

### 文件命名

统一格式：`YYYY-MM-DD_电话会_关键词.md`（日期在前，类型居中，关键词在后），严禁 `电话会_YYYY-MM-DD_...` 倒置格式。

### 阶段五 IMA沉淀执行细则

**步骤顺序**：先写IMA笔记（5A），再写本地md文件（5B），最后同步md文件到IMA（5C）。

#### 5A. 创建笔记并关联知识库

Python直接调用IMA API（header: `ima-openapi-clientid` / `ima-openapi-apikey`，非 X-IMA 前缀），两步完成：

1. `POST /openapi/note/v1/import_doc` → 获得 `note_id`
2. `POST /openapi/wiki/v1/add_knowledge`（`media_type=11`，`note_info.content_id=<note_id>`，**必须带 `folder_id`**）→ 关联到聚宝盆

#### 5C. 同步md文件到IMA

笔记关联后，将本地分析md文件同步上传至同一文件夹：

1. `POST /openapi/wiki/v1/create_media` → 获得 `media_id` + COS临时凭证
2. COS PUT上传（sha1签名 + `x-cos-security-token`，content_type 用 preflight 返回值）
3. `POST /openapi/wiki/v1/add_knowledge`（`media_type=7`，**必须带 `folder_id`）

### 文件夹路径映射

| 本地路径 | IMA folder_id | 状态 |
|----------|--------------|------|
| 浑水调研/ | `folder_7471884757106786` | ✅ |
| 深度研究/ | `folder_7471884144740883` | ✅ |
| 浑水调研/0608-0614/ | `folder_7471884761303597` | 上周 |
| 浑水调研/0615-0621/ | `folder_7472255344848188` | ✅ 当前周 |
| 深度研究/0615-0621/ | `folder_7472132342687156` | ✅ 当前周 |

> 每周新建周文件夹时需同步创建IMA对应子文件夹并记录folder_id。

### ⛔ 关键禁止

| # | 规则 |
|---|------|
| 🆕 | `add_knowledge` 禁止缺 `folder_id` —— 会导致笔记散落根目录 |
| 🆕 | 文件命名禁止 `电话会_日期_...` 格式 —— 统一 `日期_电话会_...` |
| 🆕 | 产出后必须自检：对比本地md vs IMA文件夹内容，缺文件立即补传 |

### 🔍 产出后自检（每次电话会分析完成后自动执行）

对比 `浑水调研/<周文件夹>/` 本地 .md 文件 ↔ IMA对应 `folder_id` 下 `media_type=7` 的文件。缺则补传，不需要等老大提醒。

---

## 禁止行为

| # | 禁止项 |
|---|--------|
| 1 | 每天生成独立MD文件（全部进月度总账） |
| 2 | 跳过信源交叉验证直接给结论 |
| 3 | 对C级信源标注A级 |
| 4 | 遗漏电话会日期导致时间线混乱 |
| 5 | 跳过IMA沉淀环节 |
| 6 | **精简/压缩研究内容** — IMA笔记必须包含完整分析细节，禁止只写摘要或结论列表 |
| 7 | **省略瓶颈分析维度的任何一行** — 卡点/不可替代性/供给刚性/扩产周期/价格传导必须全部覆盖 |
| 8 | **省略信源验证表中的任何一条** — 每条结论的信源等级和验证渠道都必须列出 |

## 检索协议

当用户需要查找已有研究时，按以下路径检索：

### 检索决策表

| 用户问 | 检索路径 |
|--------|----------|
| "XX 行业/标的的所有研究" | ① `Grep` 聚宝盆索引.md → ② 定位文件 → ③ 读本地或 IMA 搜索原文 |
| "最近 S 级信号汇总" | 直接读索引的 S 级信号速查表 |
| "对比 A 和 B 两个产业链的瓶颈" | ① 索引定位各自文件 → ② 读原文 → ③ 并排对比 |
| "关于 XX 标的的所有结论" | IMA `search_knowledge` 语义搜索 → 跨文档聚合 |
| "最近一周/一个月做了什么研究" | 索引按日期倒序取前 N 行 |
| "某天电话会讲了什么" | 按日期定位索引 → 读原文 |

### 索引更新规则

每次完成研究并同步聚宝盆后，在 `聚宝盆索引.md` 表格最上方追加一行：

```
| YYYY-MM-DD | 标题 | 类型 | 覆盖行业/标的 | S级信号 | 位置 |
```

同时检查是否需要更新"电话会S级信号速查"表（仅当电话会分析触发S级时）。

---

## 关联Skill

- **产业链瓶颈投研skill** — 阶段二的瓶颈分析方法论来源
- **景气度扫描skill** — 阶段三的景气度框架来源
- **信源检查skill** — 阶段四的信源分级标准来源
- **IMA MCP** — 阶段五的知识库写入工具

---

## 全局检查点规则

五阶段按顺序执行，不可跳过。每个 🔴 CHECKPOINT 打勾后进入下一阶段。🛑 STOP 触发时必须输出拦截信息，不继续后续步骤。
